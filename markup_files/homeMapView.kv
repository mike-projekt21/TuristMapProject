<HomeMapView>:
    double_tap_zoom: True
    id: map_view
    zoom:10
    lat: (55.75)
    lon: (37.61)
    on_zoom:
        self.zoom = 10 if self.zoom < 10 else self.zoom
    on_lat:
        self.start_getting_markers()
    on_lon:
        self.start_getting_markers()